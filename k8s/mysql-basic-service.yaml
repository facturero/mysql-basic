apiVersion: v1
kind: Service
metadata:
  name: mysql-basic
  labels:
    app: mysql-basic
spec:
  type: NodePort
  selector:
    app: mysql-basic
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30306
